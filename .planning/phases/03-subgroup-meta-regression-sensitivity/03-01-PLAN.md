---
phase: 03-subgroup-meta-regression-sensitivity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - meta3l/R/meta3L.R
  - meta3l/R/utils.R
  - meta3l/R/forest_helpers.R
  - meta3l/tests/testthat/helper-fixtures.R
  - meta3l/tests/testthat/test-meta3L.R
  - meta3l/tests/testthat/test-utils.R
  - meta3l/NAMESPACE
autonomous: true
requirements:
  - SUBG-01
  - SUBG-02
  - SUBG-03
  - MREG-01
  - SENS-01
  - SENS-02

must_haves:
  truths:
    - "meta3L() accepts meta-style column names (event.e, n.e, mean.e, sd.e, etc.) and auto-detects them from data"
    - "meta3L() still works with explicit escalc-style column names for backward compatibility"
    - "resolve_file() supports a suffix argument for Phase 3 filename patterns"
    - "Test fixtures include a subgroup column for Phase 3 tests"
    - "R CMD check passes with zero errors and zero warnings"
  artifacts:
    - path: "meta3l/R/meta3L.R"
      provides: "meta-style column name mapping with auto-detection"
      contains: "META_COL_MAP"
    - path: "meta3l/R/utils.R"
      provides: "Updated validate_columns supporting both naming styles"
    - path: "meta3l/R/forest_helpers.R"
      provides: "resolve_file with suffix argument"
      contains: "suffix"
    - path: "meta3l/tests/testthat/helper-fixtures.R"
      provides: "Fixtures with subgroup and continuous moderator columns"
  key_links:
    - from: "meta3l/R/meta3L.R"
      to: "meta3l/R/utils.R"
      via: "validate_columns call"
      pattern: "validate_columns"
    - from: "meta3l/R/meta3L.R"
      to: "metafor::escalc"
      via: "Column name translation before escalc call"
      pattern: "META_COL_MAP"
---

<objective>
Refactor the meta3L() API to accept meta-package-style column names (event.e, n.e, mean.e, sd.e, etc.) with smart auto-detection, extend resolve_file() with a suffix argument for Phase 3 filename patterns, and update test fixtures with subgroup/moderator columns.

Purpose: This is the foundation for all Phase 3 work. The API refactor is a locked user decision (breaking change). The resolve_file suffix and fixture updates unblock all subsequent plans.
Output: Refactored meta3L.R, extended forest_helpers.R, updated test fixtures, all existing tests passing.
</objective>

<execution_context>
@C:/Users/vanio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vanio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-subgroup-meta-regression-sensitivity/03-CONTEXT.md
@.planning/phases/03-subgroup-meta-regression-sensitivity/03-RESEARCH.md

@meta3l/R/meta3L.R
@meta3l/R/utils.R
@meta3l/R/forest_helpers.R
@meta3l/tests/testthat/helper-fixtures.R
@meta3l/tests/testthat/test-meta3L.R
@meta3l/tests/testthat/test-utils.R
@meta3l/NAMESPACE
@meta3l/DESCRIPTION

<interfaces>
<!-- Key types and contracts the executor needs -->

From meta3l/R/utils.R:
```r
REQUIRED_COLS <- list(
  PLO = c("xi", "ni"),
  PAS = c("xi", "ni"),
  RR  = c("ai", "bi", "ci", "di"),
  OR  = c("ai", "bi", "ci", "di"),
  SMD = c("m1i", "sd1i", "n1i", "m2i", "sd2i", "n2i"),
  MD  = c("m1i", "sd1i", "n1i", "m2i", "sd2i", "n2i")
)
validate_columns <- function(data, measure, col_args, slab, cluster)
resolve_transf <- function(measure, user_transf = NULL)
compute_i2 <- function(fit, V)
```

From meta3l/R/forest_helpers.R:
```r
resolve_file <- function(x, file, format)  # currently no suffix arg
auto_dims <- function(n, user_w = NULL, user_h = NULL)
format_mlab <- function(i2)
```

From meta3l/R/meta3L.R:
```r
meta3L <- function(data, slab, measure, cluster = "studlab", rho = 0.5,
                   transf = NULL, xi = NULL, ni = NULL, ai = NULL, bi = NULL,
                   ci = NULL, di = NULL, n1i = NULL, n2i = NULL,
                   m1i = NULL, sd1i = NULL, m2i = NULL, sd2i = NULL,
                   mi = NULL, sdi = NULL, name = NULL)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor meta3L() API for meta-style column names with auto-detection</name>
  <files>meta3l/R/meta3L.R, meta3l/R/utils.R, meta3l/tests/testthat/helper-fixtures.R, meta3l/tests/testthat/test-meta3L.R, meta3l/tests/testthat/test-utils.R</files>
  <action>
  1. In meta3L.R, add a META_COL_MAP list (not exported) that maps meta-package-style argument names to escalc canonical names per measure:
     ```
     META_COL_MAP <- list(
       PLO = list(xi = "event", ni = "n"),
       PAS = list(xi = "event", ni = "n"),
       RR  = list(ai = "event.e", n1i = "n.e", ci = "event.c", n2i = "n.c"),
       OR  = list(ai = "event.e", n1i = "n.e", ci = "event.c", n2i = "n.c"),
       SMD = list(m1i = "mean.e", sd1i = "sd.e", n1i = "n.e", m2i = "mean.c", sd2i = "sd.c", n2i = "n.c"),
       MD  = list(m1i = "mean.e", sd1i = "sd.e", n1i = "n.e", m2i = "mean.c", sd2i = "sd.c", n2i = "n.c")
     )
     ```

  2. Replace the escalc-style parameter names in meta3L() signature with meta-style names:
     - PLO/PAS: `event = NULL, n = NULL` (instead of xi, ni)
     - RR/OR: `event.e = NULL, n.e = NULL, event.c = NULL, n.c = NULL` (instead of ai, bi, ci, di, n1i, n2i)
     - SMD/MD: `mean.e = NULL, sd.e = NULL, n.e = NULL, mean.c = NULL, sd.c = NULL, n.c = NULL` (instead of m1i, sd1i, n1i, m2i, sd2i, n2i)
     - Keep the old escalc-style names as deprecated aliases (if user passes xi= it still works)
     - The signature should have BOTH sets of params: new meta-style first, old escalc-style deprecated

  3. Add auto-detection logic: If user provides NO column arguments, check if data has standard meta-style column names (e.g., "event.e" %in% names(data)) and use them automatically. This means `meta3L(data, slab="studlab", measure="RR")` works when data already has event.e, n.e, event.c, n.c columns.

  4. For RR/OR: When user provides event.e and n.e (or auto-detected), compute bi = n.e - event.e and di = n.c - event.c internally before the escalc call. escalc requires ai, bi, ci, di — the meta-style API only exposes events and totals.

  5. Inside meta3L(), after resolving which column names to use (meta-style, escalc-style, or auto-detected), translate to escalc canonical names and proceed with existing logic. The internal flow after column resolution should be identical to current behavior.

  6. Update helper-fixtures.R:
     - Add a `subgroup` column (character) to make_smd_data() and make_rr_data(): e.g., rep(c("Group A", "Group B"), each = 3L). Need at least 3 studies (9+ rows) for subgroup analysis to work. Expand fixtures to 3 studies (9 rows) with 3 effects each.
     - Add a continuous `dose` column (numeric) to make_smd_data() for bubble plot testing.
     - Create make_plo_data_subgroup() with 3 studies, subgroup column, 9+ rows.
     - Ensure all fixtures have studlab column as cluster.

  7. Update test-meta3L.R:
     - Add tests for meta-style column names: meta3L(data, slab="studlab", measure="RR", event.e="event.e", n.e="n.e", event.c="event.c", n.c="n.c") works.
     - Add auto-detection test: create data with standard meta-style column names, call meta3L(data, slab="studlab", measure="RR") with no column args, verify it succeeds.
     - Keep existing tests working (backward compatibility with escalc-style names).

  8. Update roxygen documentation for meta3L() to document the new parameter names and auto-detection behavior.

  CRITICAL CONSTRAINTS:
  - R >= 4.0 compatibility: NO native pipe |> or lambda \(x) syntax anywhere
  - All stop()/warning() calls use call. = FALSE
  - Do not break any existing test that uses the old parameter names
  </action>
  <verify>
    <automated>cd meta3l && Rscript -e "devtools::test(filter='test-meta3L')" 2>&1 | tail -20</automated>
  </verify>
  <done>meta3L() accepts meta-style column names, auto-detects standard names from data, computes bi/di for RR/OR internally, backward compatible with escalc-style names, all meta3L tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Extend resolve_file() with suffix, update NAMESPACE, run R CMD check</name>
  <files>meta3l/R/forest_helpers.R, meta3l/R/forest.meta3L.R, meta3l/NAMESPACE</files>
  <action>
  1. In forest_helpers.R, add a `suffix = ""` parameter to resolve_file():
     ```r
     resolve_file <- function(x, file, format, suffix = "") {
       if (is.null(file)) return(NULL)
       if (length(file) == 1L && nchar(file) > 0L) return(file)
       base_name <- if (!is.null(x$name) && nchar(x$name) > 0L) x$name else "meta3l_plot"
       fname <- if (nchar(suffix) > 0L) paste0(base_name, "_", suffix) else base_name
       dir_path <- getOption("meta3l.mwd", default = getwd())
       file.path(dir_path, paste0(fname, ".", format))
     }
     ```
     - Change default base_name from "forest_plot" to "meta3l_plot" (more generic for Phase 3 functions)
     - Existing callers (forest.meta3L) pass no suffix, so they get current behavior

  2. Update forest.meta3L.R call to resolve_file() — it already passes 3 args, adding suffix="" default means no change needed. But verify the call site matches.

  3. Run roxygenise to regenerate NAMESPACE. Ensure new Phase 3 imports will be needed:
     - `@importFrom metafor predict.rma` (for bubble plot CI band — add in bubble.meta3L.R later, not here)
     - For now, just ensure NAMESPACE is clean after the meta3L.R refactor (new roxygen tags if any)

  4. Run full test suite: `devtools::test()` — all existing tests must pass.

  5. Run `R CMD check --no-manual meta3l` — must produce 0 errors, 0 warnings. Notes are acceptable.
  </action>
  <verify>
    <automated>cd meta3l && Rscript -e "devtools::check(manual=FALSE, args='--no-examples')" 2>&1 | tail -30</automated>
  </verify>
  <done>resolve_file() has suffix parameter, NAMESPACE regenerated, R CMD check 0 errors 0 warnings, all 157+ tests still pass</done>
</task>

</tasks>

<verification>
- `devtools::test()` in meta3l/ passes all tests (existing + new meta-style column tests)
- `R CMD check --no-manual meta3l` returns 0 errors, 0 warnings
- meta3L(data, slab="studlab", measure="RR") auto-detects event.e/n.e/event.c/n.c from data
- meta3L(data, slab="studlab", measure="PLO", xi="xi", ni="ni") still works (backward compat)
- resolve_file(x, character(0), "png", suffix="subgroup_drug") returns "{mwd}/{name}_subgroup_drug.png"
</verification>

<success_criteria>
- meta-style column name API works for all 6 measure types (PLO, PAS, RR, OR, SMD, MD)
- Auto-detection works when data has standard column names and user passes no column args
- bi/di computed internally for RR/OR from event.e + n.e
- resolve_file suffix argument produces correct Phase 3 filename patterns
- Test fixtures have subgroup and dose columns for Phase 3 test plans
- Zero regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/03-subgroup-meta-regression-sensitivity/03-01-SUMMARY.md`
</output>
