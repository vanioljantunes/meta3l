---
phase: 01-core-model-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - meta3l/DESCRIPTION
  - meta3l/NAMESPACE
  - meta3l/LICENSE
  - meta3l/LICENSE.md
  - meta3l/.Rbuildignore
  - meta3l/R/meta3l-package.R
  - meta3l/R/utils.R
  - meta3l/R/read_multisheet_excel.R
  - meta3l/tests/testthat.R
  - meta3l/tests/testthat/helper-fixtures.R
  - meta3l/tests/testthat/test-read_multisheet_excel.R
  - meta3l/tests/testthat/test-utils.R
autonomous: true
requirements:
  - IMPT-01
  - MODL-05
  - MODL-06
  - MODL-07
  - MODL-08

must_haves:
  truths:
    - "read_multisheet_excel() reads all sheets from an Excel file and returns a named list of data.frames"
    - "read_multisheet_excel() auto-constructs studlab from author+year columns when present"
    - "read_multisheet_excel() falls back to rstudioapi::selectFile() when path is NULL and RStudio is available"
    - "resolve_transf() maps each supported measure to the correct back-transform function"
    - "resolve_transf() rejects unsupported measures (PFT) with an informative error"
    - "validate_columns() checks required columns exist for each measure type before escalc"
    - "Package scaffold passes basic R CMD check structure requirements"
  artifacts:
    - path: "meta3l/DESCRIPTION"
      provides: "Package metadata with correct Imports (metafor, readxl, rstudioapi, clubSandwich)"
      contains: "Imports"
    - path: "meta3l/R/read_multisheet_excel.R"
      provides: "Excel import function"
      exports: ["read_multisheet_excel"]
    - path: "meta3l/R/utils.R"
      provides: "Internal helpers"
      contains: "compute_i2"
    - path: "meta3l/tests/testthat/helper-fixtures.R"
      provides: "Synthetic test data for all measure types"
      contains: "fixture"
  key_links:
    - from: "meta3l/R/utils.R"
      to: "metafor::transf.ilogit"
      via: "resolve_transf switch statement"
      pattern: "transf\\.ilogit"
    - from: "meta3l/R/utils.R"
      to: "metafor::escalc"
      via: "REQUIRED_COLS list matching escalc argument names"
      pattern: "REQUIRED_COLS"
    - from: "meta3l/R/read_multisheet_excel.R"
      to: "readxl::read_excel"
      via: "lapply over excel_sheets"
      pattern: "read_excel"
---

<objective>
Scaffold the meta3l R package and implement the Excel import function plus all internal helper utilities that meta3L() will depend on.

Purpose: Establish the package structure with correct CRAN-compliant DESCRIPTION, NAMESPACE via roxygen2, and the foundational internal functions (compute_i2, resolve_transf, validate_columns) that the core meta3L() function will call. Also implements read_multisheet_excel() as a standalone exported function.

Output: A valid R package skeleton with working read_multisheet_excel(), all three internal helpers, test fixtures for every measure type, and passing tests for import + utils.
</objective>

<execution_context>
@C:/Users/vanio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vanio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-model-pipeline/01-CONTEXT.md
@.planning/phases/01-core-model-pipeline/01-RESEARCH.md
@three_level.Rmd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold package and implement read_multisheet_excel</name>
  <files>meta3l/DESCRIPTION, meta3l/NAMESPACE, meta3l/LICENSE, meta3l/LICENSE.md, meta3l/.Rbuildignore, meta3l/R/meta3l-package.R, meta3l/R/read_multisheet_excel.R, meta3l/tests/testthat.R</files>
  <action>
Create the meta3l package directory structure manually (do NOT use usethis::create_package since we are not in an R session — write each file directly).

1. **meta3l/DESCRIPTION**: Package name `meta3l`, Title "Three-Level Meta-Analysis via metafor", Version 0.1.0, Depends R >= 4.0.0, Imports: metafor (>= 4.0-0), readxl (>= 1.3.0), rstudioapi (>= 0.13), clubSandwich (>= 0.5.0). Suggests: testthat (>= 3.0.0). License: MIT + file LICENSE. Encoding: UTF-8. RoxygenNote: 7.3.2. Config/testthat/edition: 3. Author field: use person() with the user's name placeholder and [aut, cre] role.

2. **meta3l/LICENSE**: Single line "MIT + file LICENSE" is for DESCRIPTION; the LICENSE file itself should contain the year and copyright holder template. Also create LICENSE.md with full MIT text.

3. **meta3l/.Rbuildignore**: Standard entries — ^meta3l\\.Rproj$, ^\.Rproj\.user$, ^LICENSE\.md$

4. **meta3l/R/meta3l-package.R**: Package-level roxygen block with @keywords internal, @aliases meta3l-package, @importFrom metafor escalc vcalc rma.mv robust transf.ilogit transf.iarcsin, @importFrom readxl excel_sheets read_excel, @importFrom stats as.formula model.matrix. Include `utils::globalVariables(c("yi", "vi", "TE_id"))` at the top level for CRAN NOTE suppression.

5. **meta3l/R/read_multisheet_excel.R**: Implement `read_multisheet_excel(path = NULL)` with full roxygen docs (@export, @param, @return, @examples with \dontrun{}). Logic:
   - If path is NULL: check `rstudioapi::isAvailable()`; if TRUE call `rstudioapi::selectFile(caption = "Choose Excel file", filter = "Excel files (*.xlsx, *.xls)")`; if FALSE, `stop("path argument is required in non-RStudio environments.", call. = FALSE)`
   - Get sheet names via `readxl::excel_sheets(path)`
   - lapply over sheets: `as.data.frame(readxl::read_excel(path, sheet = s))`
   - If nrow == 0: `warning("Sheet '", s, "' is empty and will be skipped.", call. = FALSE)` and return NULL
   - If "author" and "year" columns both present: `df$studlab <- paste0(df$author, ", ", df$year)`
   - Set names to sheet names, Filter out NULLs, return named list
   - Use `function(x)` syntax, NOT `\(x)` lambda (R >= 4.0 compat per MODL-08)

6. **meta3l/tests/testthat.R**: Standard test runner: `library(testthat); library(meta3l); test_check("meta3l")`

Do NOT generate NAMESPACE manually — it will be generated by roxygen2 in the verification step. Create an empty meta3l/man/ directory placeholder (or .gitkeep in man/).
  </action>
  <verify>
    <automated>cd meta3l && Rscript -e "roxygen2::roxygenize('.')" && Rscript -e "cat(readLines('NAMESPACE'), sep='\n')" && Rscript -e "if (!file.exists('R/read_multisheet_excel.R')) stop('missing file')"</automated>
  </verify>
  <done>meta3l/ directory exists with valid DESCRIPTION, roxygen2 generates NAMESPACE without error, read_multisheet_excel.R has proper roxygen docs and exports, package-level doc suppresses global variable NOTEs</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement internal helpers (utils.R) and test fixtures</name>
  <files>meta3l/R/utils.R, meta3l/tests/testthat/helper-fixtures.R, meta3l/tests/testthat/test-utils.R, meta3l/tests/testthat/test-read_multisheet_excel.R</files>
  <behavior>
    - resolve_transf("PLO") returns metafor::transf.ilogit
    - resolve_transf("PAS") returns metafor::transf.iarcsin
    - resolve_transf("RR") returns exp; resolve_transf("OR") returns exp
    - resolve_transf("SMD") returns identity; resolve_transf("MD") returns identity
    - resolve_transf("PFT") throws error matching "not supported"
    - resolve_transf("PLO", user_transf = sqrt) returns sqrt (user override)
    - validate_columns errors when required column is missing from data
    - validate_columns errors when slab column is missing
    - validate_columns passes silently when all columns present
    - compute_i2 returns list with total, between, within components (numeric, non-negative)
    - compute_i2: between + within equals total (within floating point tolerance)
  </behavior>
  <action>
1. **meta3l/R/utils.R**: Implement three internal functions (no @export, but with roxygen @keywords internal docs):

   a) `resolve_transf(measure, user_transf = NULL)` — If user_transf is not NULL, return it. Otherwise switch on measure: PLO = metafor::transf.ilogit, PAS = metafor::transf.iarcsin, RR = exp, OR = exp, SMD = identity, MD = identity. Default case: `stop("measure '", measure, "' is not supported. Use PLO, PAS, SMD, MD, RR, or OR.", call. = FALSE)`. Use standard `switch()` — no `\(x)` lambda.

   b) `validate_columns(data, measure, col_args, slab, cluster)` — Define REQUIRED_COLS list: PLO/PAS = c("xi","ni"), SMD/MD with two-group = c("m1i","sd1i","n1i","m2i","sd2i","n2i"), RR/OR = c("ai","bi","ci","di"). Check measure is in names(REQUIRED_COLS); if not, stop with supported measures list. For each required col name, look up the user-supplied column name string in col_args; if NULL or not in names(data), stop with informative message naming the missing column and the measure. Also check slab and cluster columns exist in data. Note: SMD can also be single-arm (mi/sdi/ni) but per locked decision the two-group form uses m1i/sd1i/n1i/m2i/sd2i/n2i. Actually, re-reading the CONTEXT.md: SMD can be two-group (m1i/sd1i/n1i/m2i/sd2i/n2i) — this is the standard form. Keep REQUIRED_COLS for SMD/MD as the two-group form.

   c) `compute_i2(fit, V)` — Implements P-matrix projection I-squared. `W <- solve(V)`, `X <- model.matrix(fit)`, P projection matrix, denominator = sum(fit$sigma2) + (fit$k - fit$p) / sum(diag(P)). Return `list(total = 100 * sum(fit$sigma2) / denom, between = 100 * fit$sigma2[1] / denom, within = 100 * fit$sigma2[2] / denom)`. Use `solve()` NOT `diag(1/fit$vi)` — the V matrix from vcalc is non-diagonal.

2. **meta3l/tests/testthat/helper-fixtures.R**: Create synthetic minimal data frames for testing:
   - `make_plo_data()`: returns data.frame with columns author, year, studlab, event (xi), n (ni) — at least 6 rows, 2 studies with 3 effects each
   - `make_pas_data()`: same structure as PLO (same columns work for PAS)
   - `make_smd_data()`: data.frame with studlab, m1i, sd1i, n1i, m2i, sd2i, n2i — 6 rows, 2 studies
   - `make_md_data()`: same structure as SMD
   - `make_rr_data()`: data.frame with studlab, ai, bi, ci, di — 6 rows, 2 studies
   - `make_or_data()`: same structure as RR
   - All fixtures must have realistic numeric values that will not cause metafor errors (e.g., non-zero counts, reasonable SDs)
   - Also create `make_excel_fixture(path)` that writes a test Excel file with 2 sheets using openxlsx or writexl (add a note that test needs writexl installed). Actually, since writexl may not be available, create the fixture using `openxlsx::write.xlsx()` or check if available. Simpler approach: skip the Excel write in tests and test read_multisheet_excel with a pre-created .xlsx in inst/testdata/. Better yet: write the test to create a temp xlsx using writexl (add writexl to Suggests for testing only), or use openxlsx. Use whichever is simpler. The test should be skipped if neither is available: `testthat::skip_if_not_installed("writexl")`.

3. **meta3l/tests/testthat/test-utils.R**: TDD tests for all three internal helpers per the behavior spec above. Use `testthat::test_that()` blocks. For compute_i2, create a minimal rma.mv fit using the PLO fixture data, then verify the return structure and that between + within = total.

4. **meta3l/tests/testthat/test-read_multisheet_excel.R**: Test that read_multisheet_excel() reads a temp Excel file and returns named list of data.frames. Test studlab auto-construction. Test that empty path without RStudio errors. Use `skip_if_not_installed("writexl")` for the file-based tests.

Remember: NO native pipe `|>` or `\(x)` lambda anywhere in R code (MODL-08).
  </action>
  <verify>
    <automated>cd meta3l && Rscript -e "roxygen2::roxygenize('.')" && Rscript -e "devtools::test(pkg='.', reporter='summary')"</automated>
  </verify>
  <done>All resolve_transf tests pass (6 measures + PFT rejection + user override). validate_columns tests pass (missing col errors, slab check, happy path). compute_i2 returns correct structure with between+within=total. read_multisheet_excel tests pass (file read, studlab construction, no-path error).</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd meta3l && Rscript -e "roxygen2::roxygenize('.')"` runs without error
2. `cd meta3l && Rscript -e "devtools::test(pkg='.', reporter='summary')"` shows all tests passing
3. NAMESPACE contains exports for read_multisheet_excel and S3 methods placeholder
4. No `|>` or `\(x)` syntax anywhere in R/ directory
</verification>

<success_criteria>
- meta3l/ package directory exists with valid DESCRIPTION, LICENSE, .Rbuildignore
- read_multisheet_excel() exported with full roxygen docs, handles NULL path with rstudioapi guard
- resolve_transf() correctly maps all 6 measures and rejects PFT
- validate_columns() checks all required columns per measure type
- compute_i2() implements P-matrix projection with solve(V)
- Test fixtures exist for all 6 measure types
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-model-pipeline/01-01-SUMMARY.md`
</output>
