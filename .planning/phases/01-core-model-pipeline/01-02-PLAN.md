---
phase: 01-core-model-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - meta3l/R/meta3L.R
  - meta3l/R/print.meta3l_result.R
  - meta3l/R/summary.meta3l_result.R
  - meta3l/R/meta3l-package.R
  - meta3l/tests/testthat/test-meta3L.R
autonomous: true
requirements:
  - MODL-01
  - MODL-02
  - MODL-03
  - MODL-04
  - MODL-08

must_haves:
  truths:
    - "User can call meta3L() with a data frame and measure argument and receive a meta3l_result object"
    - "meta3l_result contains a fitted rma.mv model accessible via result$model"
    - "meta3l_result contains three I-squared values (total, between, within) that are numeric and non-negative"
    - "meta3l_result contains robust variance estimates from clubSandwich CR2"
    - "meta3l_result contains the resolved back-transform function"
    - "meta3l_result contains pre-computed back-transformed pooled estimate and CI"
    - "print() displays pooled estimate, CI, k, n, rho, I-squared breakdown"
    - "summary() shows everything in print plus sigma-squared components and robust variance table"
    - "Unsupported measure (PFT) produces informative stop() at meta3L() call"
    - "NA rows in essential columns are filtered with a warning"
    - "Package passes R CMD check --as-cran with zero errors and zero warnings"
  artifacts:
    - path: "meta3l/R/meta3L.R"
      provides: "Core meta3L() function"
      exports: ["meta3L"]
      min_lines: 80
    - path: "meta3l/R/print.meta3l_result.R"
      provides: "S3 print method"
      exports: ["print.meta3l_result"]
    - path: "meta3l/R/summary.meta3l_result.R"
      provides: "S3 summary method"
      exports: ["summary.meta3l_result"]
    - path: "meta3l/tests/testthat/test-meta3L.R"
      provides: "Core function tests"
      min_lines: 40
  key_links:
    - from: "meta3l/R/meta3L.R"
      to: "meta3l/R/utils.R"
      via: "calls validate_columns, resolve_transf, compute_i2"
      pattern: "validate_columns|resolve_transf|compute_i2"
    - from: "meta3l/R/meta3L.R"
      to: "metafor::rma.mv"
      via: "three-level model fit with dynamic random formula"
      pattern: "rma\\.mv"
    - from: "meta3l/R/meta3L.R"
      to: "metafor::robust"
      via: "CR2 robust variance estimation"
      pattern: "robust.*clubSandwich"
    - from: "meta3l/R/print.meta3l_result.R"
      to: "meta3l_result S3 class"
      via: "S3method dispatch"
      pattern: "print\\.meta3l_result"
---

<objective>
Implement the core meta3L() function, S3 print/summary methods, and comprehensive tests. Achieve R CMD check --as-cran compliance.

Purpose: This is the central deliverable of Phase 1 — the single-call three-level meta-analysis function that wraps escalc, vcalc, rma.mv, robust, and I-squared computation into one API. Combined with Plan 01's helpers, this completes the entire Phase 1 pipeline.

Output: Working meta3L() that handles all 6 measure types, print/summary S3 methods, full test suite, zero-error R CMD check.
</objective>

<execution_context>
@C:/Users/vanio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/vanio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-model-pipeline/01-CONTEXT.md
@.planning/phases/01-core-model-pipeline/01-RESEARCH.md
@.planning/phases/01-core-model-pipeline/01-01-SUMMARY.md
@three_level.Rmd

<interfaces>
<!-- From Plan 01 — internal helpers the executor will call -->

From meta3l/R/utils.R:
```r
# Internal: resolve back-transform function from measure string
# Returns a function (e.g., metafor::transf.ilogit, exp, identity)
resolve_transf <- function(measure, user_transf = NULL)

# Internal: validate required columns exist in data for given measure
# col_args is a named list mapping escalc arg names to user-supplied column name strings
validate_columns <- function(data, measure, col_args, slab, cluster)

# Internal: P-matrix projection I-squared for three-level model
# Returns list(total, between, within) as percentages
compute_i2 <- function(fit, V)

# Constant: required escalc columns per measure type
REQUIRED_COLS  # list with PLO, PAS, SMD, MD, RR, OR keys
```

From meta3l/tests/testthat/helper-fixtures.R:
```r
make_plo_data()  # data.frame with author, year, studlab, event, n (6 rows, 2 studies)
make_pas_data()  # same structure
make_smd_data()  # data.frame with studlab, m1i, sd1i, n1i, m2i, sd2i, n2i
make_md_data()   # same as SMD
make_rr_data()   # data.frame with studlab, ai, bi, ci, di
make_or_data()   # same as RR
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement meta3L() core function and S3 methods</name>
  <files>meta3l/R/meta3L.R, meta3l/R/print.meta3l_result.R, meta3l/R/summary.meta3l_result.R, meta3l/R/meta3l-package.R, meta3l/tests/testthat/test-meta3L.R</files>
  <behavior>
    - meta3L(data, slab="studlab", xi="event", ni="n", measure="PLO") returns object of class "meta3l_result"
    - result$model inherits from "robust.rma"
    - result$i2 is a list with numeric total, between, within; between + within == total (tolerance 0.01)
    - result$V is a symmetric matrix with nrow == nrow of escalc'd data
    - result$transf is a function (for PLO: metafor::transf.ilogit)
    - result$estimate, result$ci.lb, result$ci.ub are numeric scalars (back-transformed)
    - result$measure == "PLO", result$cluster == "studlab", result$rho == 0.5
    - meta3L with measure="PAS" runs without error and returns meta3l_result
    - meta3L with measure="RR" using make_rr_data() runs without error
    - meta3L with measure="PFT" throws error matching "not supported"
    - meta3L with missing required column throws error matching column name
    - meta3L with NA rows in xi column warns about dropped rows
    - print(result) produces output containing "Pooled", "I", "between", "within"
    - summary(result) produces output containing "sigma" (sigma-squared components)
    - meta3L with custom cluster argument uses that column for grouping
  </behavior>
  <action>
1. **Write tests first** in `meta3l/tests/testthat/test-meta3L.R`. Use the fixture functions from helper-fixtures.R. Key test blocks:
   - "meta3L returns meta3l_result for PLO": call meta3L on make_plo_data(), check class, check result$model class, check result$i2 structure, check result$transf is function, check result$estimate is numeric
   - "meta3L works for PAS measure": same pattern with make_pas_data()
   - "meta3L works for RR measure": make_rr_data() with appropriate column args
   - "meta3L rejects PFT": expect_error(meta3L(..., measure="PFT"), "not supported")
   - "meta3L errors on missing column": expect_error with a mangled column name
   - "meta3L warns on NA rows": add NAs to fixture data, expect_warning
   - "print.meta3l_result produces expected output": capture.output and grep for key strings
   - "summary.meta3l_result shows sigma2": capture.output and grep
   - "meta3L respects custom cluster": use make_plo_data() with a different cluster column name

2. **Implement meta3L()** in `meta3l/R/meta3L.R` with full roxygen docs (@export, @param for every argument, @return, @examples with \dontrun{}):

   Function signature: `meta3L(data, slab, measure, cluster = "studlab", rho = 0.5, transf = NULL, xi = NULL, ni = NULL, ai = NULL, bi = NULL, ci = NULL, di = NULL, n1i = NULL, n2i = NULL, m1i = NULL, sd1i = NULL, m2i = NULL, sd2i = NULL, mi = NULL, sdi = NULL)`

   Implementation steps (all using `function(x)` NOT `\(x)`, no `|>` pipe):
   a) Build col_args named list from non-NULL column arguments
   b) Call `validate_columns(data, measure, col_args, slab, cluster)`
   c) Call `resolve_transf(measure, transf)` to get transf_fn
   d) Filter NA rows: for each required column (looked up from REQUIRED_COLS), check for NAs in `data[[col_args[[req]]]]`. Count NAs, warn if any, filter them out.
   e) Build escalc args dynamically using `do.call()`: create a list with measure, data, slab (as formula or direct). Key subtlety from RESEARCH pitfall 4: rename user columns to standard escalc names before calling escalc, OR use do.call with the column vectors. Safest approach: create a working copy of data, rename columns to match escalc expectations (xi, ni, etc.), then call `escalc(measure=measure, xi=xi, ni=ni, data=dat_work, slab=studlab)` with standard names. This avoids the vector-vs-name pitfall.
   f) Add `dat$TE_id <- seq_len(nrow(dat))` to escalc'd data
   g) Build V matrix: `metafor::vcalc(vi = dat$vi, cluster = dat[[cluster]], obs = dat$TE_id, rho = rho, data = dat)`
   h) Build random formula dynamically: `as.formula(paste0("~ 1 | ", cluster, " / TE_id"))`
   i) Fit model: `metafor::rma.mv(yi, V, random = random_formula, data = dat)`
   j) Robust: `metafor::robust(res, cluster = dat[[cluster]], clubSandwich = TRUE)`
   k) I-squared: `compute_i2(res, V)` (use the non-robust fit for I-squared — sigma2 comes from rma.mv, not robust)
   l) Back-transform pooled estimate and CI from robust result
   m) Construct and return meta3l_result S3 object using `structure()` with all fields per CONTEXT.md spec

3. **Implement print.meta3l_result()** in `meta3l/R/print.meta3l_result.R`:
   - @export, @method print meta3l_result
   - Display: "Three-Level Meta-Analysis (meta3l)" header
   - Pooled estimate + 95% CI (back-transformed), formatted to 4 decimal places
   - k studies (number of unique clusters), n effect sizes (nrow of data)
   - Assumed rho value
   - I-squared breakdown: total, between-cluster, within-cluster (formatted to 1 decimal)
   - Note: "Robust SEs via clubSandwich (CR2)"
   - Return x invisibly

4. **Implement summary.meta3l_result()** in `meta3l/R/summary.meta3l_result.R`:
   - @export, @method summary meta3l_result
   - Everything from print, plus:
   - Sigma-squared components (fit$sigma2[1] for between, fit$sigma2[2] for within)
   - Robust variance table (from robust model: coefficient, SE, CI, z-value, p-value)
   - Model convergence info (fit$convergence or similar from rma.mv)
   - Return x invisibly

5. **Update meta3l-package.R** if needed: ensure any additional imports required by meta3L.R are declared (e.g., @importFrom stats as.formula model.matrix).

CRITICAL constraints (MODL-08):
- NO `|>` native pipe anywhere
- NO `\(x)` lambda syntax — use `function(x)`
- NO `:::` calls to internal functions
- Use `seq_len()` not `1:n`
  </action>
  <verify>
    <automated>cd meta3l && Rscript -e "roxygen2::roxygenize('.')" && Rscript -e "devtools::test(pkg='.', reporter='summary')"</automated>
  </verify>
  <done>meta3L() returns correct meta3l_result for PLO, PAS, SMD, RR, OR measures. PFT rejected. Missing columns caught. NA rows warned and filtered. print() shows pooled estimate, I-squared, k, n. summary() shows sigma-squared. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: R CMD check --as-cran compliance</name>
  <files>meta3l/R/meta3l-package.R, meta3l/DESCRIPTION, meta3l/R/meta3L.R</files>
  <action>
Run `devtools::check(args = "--as-cran")` and fix ALL errors and warnings. Common issues to proactively address:

1. **Global variable bindings**: Ensure `utils::globalVariables(c("yi", "vi", "TE_id"))` is in meta3l-package.R. If check reveals additional unbound variables, add them.

2. **Missing roxygen imports**: Every external function used must have an @importFrom or the package must have @import. Prefer @importFrom for specificity. Check that `solve`, `identity` (base R) don't need explicit imports (they don't — they're in base).

3. **S3 method registration**: Verify NAMESPACE has `S3method(print, meta3l_result)` and `S3method(summary, meta3l_result)`. The @method tag in roxygen should produce this.

4. **Examples**: All @examples blocks should use \dontrun{} since they require metafor to be installed and data to be available. Alternatively, use \donttest{} for examples that work but are slow.

5. **DESCRIPTION completeness**: Ensure Authors@R field is properly formatted, Description field is at least one full sentence ending with a period.

6. **No native pipe or lambda**: grep all .R files for `|>` and `\\(` patterns — fix any violations.

7. **Run check iteratively**: fix issues, re-run, until zero errors and zero warnings. NOTEs are acceptable if they are only "New submission" or "checking CRAN incoming feasibility" — these are informational for first-time packages.

If any test failures emerge during check that were not caught in Task 1, fix the underlying code (not just suppress the test).
  </action>
  <verify>
    <automated>cd meta3l && Rscript -e "roxygen2::roxygenize('.')" && Rscript -e "res <- devtools::check(pkg='.', args='--as-cran', quiet=TRUE); if (length(res$errors) > 0 || length(res$warnings) > 0) stop('Check failed: ', paste(c(res$errors, res$warnings), collapse='\n'))"</automated>
  </verify>
  <done>R CMD check --as-cran produces zero errors and zero warnings. All tests pass within check. NAMESPACE is correct. No |> or \(x) syntax in any R file.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd meta3l && Rscript -e "devtools::test(pkg='.', reporter='summary')"` — all tests pass
2. `cd meta3l && Rscript -e "res <- devtools::check(pkg='.', args='--as-cran', quiet=TRUE); cat('Errors:', length(res$errors), 'Warnings:', length(res$warnings), '\n')"` — 0 errors, 0 warnings
3. `Rscript -e "devtools::load_all('meta3l'); d <- data.frame(author=rep('A',4), year=rep(2020,4), studlab=rep(c('A, 2020','B, 2020'),each=2), event=c(10,12,8,15), n=c(50,60,40,70)); r <- meta3L(d, slab='studlab', xi='event', ni='n', measure='PLO'); print(r)"` — produces readable output with pooled estimate and I-squared
</verification>

<success_criteria>
- meta3L() works for all 6 supported measure types (PLO, PAS, SMD, MD, RR, OR)
- meta3l_result S3 object contains all specified fields (model, data, V, i2, transf, measure, cluster, rho, slab, TE_id, estimate, ci.lb, ci.ub)
- print() and summary() produce the specified output format
- PFT and unsupported measures fail with informative errors
- NA rows filtered with warning
- Custom cluster column works
- R CMD check --as-cran: zero errors, zero warnings
- All code uses function() not \(x), no |>, no :::
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-model-pipeline/01-02-SUMMARY.md`
</output>
