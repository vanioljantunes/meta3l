---
phase: 01-core-model-pipeline
plan: 01
subsystem: api
tags: [r-package, metafor, readxl, rstudioapi, clubSandwich, testthat, roxygen2, tdd]

# Dependency graph
requires: []
provides:
  - meta3l R package scaffold (DESCRIPTION, NAMESPACE, LICENSE, .Rbuildignore)
  - read_multisheet_excel() exported function with rstudioapi guard for NULL path
  - resolve_transf() internal helper mapping 6 measures to back-transform functions
  - validate_columns() internal helper checking required escalc columns per measure
  - compute_i2() internal helper using P-matrix projection with solve(V)
  - test fixtures for all 6 measure types (PLO, PAS, SMD, MD, RR, OR)
  - 29 passing testthat tests
affects:
  - 01-02 (meta3L core function will call resolve_transf, validate_columns, compute_i2)
  - 02-forest-plot (will consume meta3l_result from 01-02 built on these utilities)
  - 03-subgroup-moderator (will consume meta3l_result and compute_i2 directly)

# Tech tracking
tech-stack:
  added:
    - roxygen2 7.3.3 (NAMESPACE + Rd generation)
    - metafor >= 4.0-0 (escalc, vcalc, rma.mv, robust, transf.ilogit, transf.iarcsin)
    - readxl >= 1.3.0 (excel_sheets, read_excel)
    - rstudioapi >= 0.13 (isAvailable, selectFile)
    - clubSandwich >= 0.5.0 (robust variance estimation)
    - testthat >= 3.0.0 (test suite)
    - writexl (Suggests; used in read_multisheet_excel tests)
  patterns:
    - R >= 4.0 compat: no native pipe (|>) or lambda (\(x)) in any R/ source
    - NAMESPACE generated by roxygen2 only — never edited manually
    - Internal helpers documented with @keywords internal (not exported)
    - REQUIRED_COLS list in utils.R is the single source of truth for per-measure column requirements
    - P-matrix projection I2 uses solve(V) as precision matrix (non-diagonal V from vcalc)
    - TDD: failing tests committed before implementation

key-files:
  created:
    - meta3l/DESCRIPTION
    - meta3l/NAMESPACE
    - meta3l/LICENSE
    - meta3l/LICENSE.md
    - meta3l/.Rbuildignore
    - meta3l/R/meta3l-package.R
    - meta3l/R/read_multisheet_excel.R
    - meta3l/R/utils.R
    - meta3l/tests/testthat.R
    - meta3l/tests/testthat/helper-fixtures.R
    - meta3l/tests/testthat/test-utils.R
    - meta3l/tests/testthat/test-read_multisheet_excel.R
    - meta3l/man/read_multisheet_excel.Rd
    - meta3l/man/meta3l-package.Rd
    - meta3l/man/resolve_transf.Rd
    - meta3l/man/validate_columns.Rd
    - meta3l/man/compute_i2.Rd
  modified: []

key-decisions:
  - "R >= 4.0 compat enforced: no |> or \\(x) lambda in any R/ source (MODL-08)"
  - "REQUIRED_COLS list in utils.R is single source of truth for per-measure column requirements; maps PLO/PAS->xi/ni, SMD/MD->two-group m1i/sd1i/n1i/m2i/sd2i/n2i, RR/OR->ai/bi/ci/di"
  - "compute_i2 uses solve(V) as precision matrix (W = solve(V)) matching metafor documentation for non-diagonal V from vcalc"
  - "resolve_transf uses switch() not if-else chain; user_transf override checked first"
  - "read_multisheet_excel returns named list of data.frames (not tibbles); filters empty sheets with warning"
  - "studlab auto-construction uses paste0(author, ', ', year) when both columns present; silently skipped otherwise"

patterns-established:
  - "R >= 4.0 compat: use function(x) not \\(x), no native pipe |>"
  - "switch() for measure dispatch in resolve_transf"
  - "REQUIRED_COLS list pattern for per-measure column validation"
  - "P-matrix I2: W <- solve(V); P <- W - W %*% X %*% solve(t(X) %*% W %*% X) %*% t(X) %*% W"
  - "TDD: write failing tests first, commit as test(), then implement and commit as feat()"

requirements-completed: [IMPT-01, MODL-05, MODL-06, MODL-07, MODL-08]

# Metrics
duration: 5min
completed: 2026-03-10
---

# Phase 1 Plan 01: Package Scaffold and Core Utilities Summary

**meta3l R package scaffolded with read_multisheet_excel(), resolve_transf(), validate_columns(), and compute_i2() helpers; 29 tests passing via TDD**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-10T17:41:48Z
- **Completed:** 2026-03-10T17:46:51Z
- **Tasks:** 2 (Task 1: scaffold + read_multisheet_excel; Task 2: utils.R TDD)
- **Files modified:** 17 created, 0 modified

## Accomplishments
- CRAN-compliant meta3l package scaffold with DESCRIPTION (metafor, readxl, rstudioapi, clubSandwich imports), MIT LICENSE, roxygen2-generated NAMESPACE
- read_multisheet_excel() exported with rstudioapi guard for NULL path, studlab auto-construction, empty sheet warning, no-tibble return
- resolve_transf() maps PLO/PAS/RR/OR/SMD/MD to back-transform functions; rejects PFT with informative error; user_transf override respected
- validate_columns() uses REQUIRED_COLS list as single source of truth for per-measure column requirements; checks slab and cluster columns
- compute_i2() implements P-matrix projection using solve(V) as precision matrix; returns total/between/within I2 breakdown
- Test fixtures for all 6 measure types with realistic numeric values; 29 tests all passing

## Task Commits

Each task was committed atomically:

1. **Task 1: Scaffold package and implement read_multisheet_excel** - `a9a8df2` (feat)
2. **Task 2 RED: Add failing tests for utils and read_multisheet_excel** - `c6791e8` (test)
3. **Task 2 GREEN: Implement internal helpers (utils.R)** - `85d6ae7` (feat)

_Note: TDD task has test commit (RED) + implementation commit (GREEN)_

## Files Created/Modified
- `meta3l/DESCRIPTION` - Package metadata with Imports and Suggests
- `meta3l/NAMESPACE` - Generated by roxygen2; exports read_multisheet_excel
- `meta3l/R/meta3l-package.R` - Package-level docs with globalVariables suppression
- `meta3l/R/read_multisheet_excel.R` - Excel import function with rstudioapi guard
- `meta3l/R/utils.R` - resolve_transf, validate_columns, compute_i2 internal helpers
- `meta3l/tests/testthat/helper-fixtures.R` - Synthetic fixtures for 6 measure types
- `meta3l/tests/testthat/test-utils.R` - 20 tests for all three helpers
- `meta3l/tests/testthat/test-read_multisheet_excel.R` - 9 tests for Excel import

## Decisions Made
- R >= 4.0 compat enforced throughout: no |> or \(x) lambda in any R/ source
- REQUIRED_COLS list in utils.R is the single source of truth for per-measure escalc columns
- SMD/MD use two-group form (m1i/sd1i/n1i/m2i/sd2i/n2i) per locked CONTEXT.md decision
- compute_i2 uses solve(V) not diag(1/vi) because V from vcalc is non-diagonal
- roxygen2-only NAMESPACE management; never edited manually

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Rscript not in PATH on Windows; resolved by using full path `/c/Program Files/R/R-4.5.2/bin/Rscript.exe` for all R commands.
- roxygen2 auto-updated RoxygenNote from 7.3.2 to 7.3.3 in DESCRIPTION (installed version is 7.3.3); not a deviation, expected behavior.

## User Setup Required
None - no external service configuration required. Package requires metafor, readxl, rstudioapi, and clubSandwich installed in R.

## Next Phase Readiness
- All utilities needed by meta3L() are implemented and tested
- resolve_transf, validate_columns, compute_i2 ready for use in 01-02 (meta3L core function)
- Test fixtures available for reuse in 01-02 tests
- Package loads cleanly via devtools::load_all()

---
*Phase: 01-core-model-pipeline*
*Completed: 2026-03-10*
