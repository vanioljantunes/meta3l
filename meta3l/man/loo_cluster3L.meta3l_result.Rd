% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loo_cluster.meta3L.R
\name{loo_cluster3L.meta3l_result}
\alias{loo_cluster3L.meta3l_result}
\title{Cluster-level leave-one-out sensitivity analysis for meta3l_result objects}
\usage{
\method{loo_cluster3L}{meta3l_result}(
  x,
  file = NULL,
  format = "png",
  width = NULL,
  height = NULL,
  title = x$name,
  shade = "zebra",
  ...
)
}
\arguments{
\item{x}{A \code{meta3l_result} object returned by \code{\link{meta3L}}.}

\item{file}{One of: \code{NULL} (default, draw on screen, write no file
and return \code{NULL} for \code{plot_file}); a character string
(explicit file path); or \code{character(0)} (auto-name from
\code{x$name} and the \code{meta3l.mwd} option).}

\item{format}{Character string; output format — \code{"png"} (default) or
\code{"pdf"}.}

\item{width}{Integer or NULL; output width in pixels.  \code{NULL}
auto-computes via \code{auto_dims()}.}

\item{height}{Integer or NULL; output height in pixels.  \code{NULL}
auto-computes.}

\item{...}{Currently ignored.}
}
\value{
Invisibly returns a named list with:
  \describe{
    \item{table}{A data frame with columns \code{omitted}, \code{estimate},
      \code{ci.lb}, \code{ci.ub}, \code{i2_between}, \code{i2_within},
      \code{pval}.  The last row is the "All studies" baseline.}
    \item{plot_file}{File path of the saved plot (character string) or
      \code{NULL} if \code{file = NULL}.}
  }
}
\description{
Drops each unique cluster (study) in turn, refits the three-level model on
the remaining data, and returns a table showing how the pooled estimate and
heterogeneity statistics change.  Also produces a grid-based influence plot.
}
