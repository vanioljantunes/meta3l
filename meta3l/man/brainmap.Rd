% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brainmap.meta3L.R
\name{brainmap}
\alias{brainmap}
\alias{brainmap.meta3l_bind}
\title{Brain map of pooled regional effects}
\usage{
brainmap(x, ...)

\method{brainmap}{meta3l_bind}(
  x,
  view = "axial_5",
  palette = "Okeeffe1",
  direction = 1,
  value = c("estimate", "abs"),
  scale = c("sequential", "diverging"),
  limits = NULL,
  region.map = NULL,
  schematic = NULL,
  labels = TRUE,
  legend.title = NULL,
  title = NULL,
  caption = TRUE,
  file = character(0),
  format = "png",
  width = NULL,
  height = NULL,
  ...
)
}
\arguments{
\item{x}{An object holding one pooled estimate per anatomical region.}

\item{...}{Passed to the method.}

\item{view}{Character; atlas view to draw.  One of the views of
\code{ggseg::aseg()}; \code{"axial_5"} (default) is the slice that carries
caudate, putamen, pallidum, thalamus and VentralDC together.}

\item{palette}{Character; \pkg{MetBrewer} palette name.  Defaults to
\code{"Okeeffe1"}.}

\item{direction}{\code{1} (default) or \code{-1}; flips the palette.}

\item{value}{One of \code{"estimate"} (default) or \code{"abs"}; \code{"abs"}
colours by the magnitude of the effect regardless of sign.}

\item{scale}{One of \code{"sequential"} (default) or \code{"diverging"}.
Most MetBrewer palettes are diverging, so their two ends are both dark and
a weak effect would look as strong as a large one; \code{"sequential"}
uses one arm of the palette, pale to dark, so a stronger colour always
means a larger effect.}

\item{limits}{Numeric length 2; colour scale limits.  \code{NULL}
auto-computes from the data.}

\item{region.map}{Named character vector mapping analysis labels to atlas
region names, for labels the automatic matcher cannot resolve.  Names are
the analysis labels.}

\item{schematic}{Character vector of length 2 giving the analysis labels to
draw as midbrain ellipses, in the order red nucleus, substantia nigra.
\code{NULL} (default) auto-detects them from the labels.  Use \code{NA} in
a slot to skip it, or \code{character(0)} to draw none.}

\item{labels}{Logical; write the region name on each coloured region
(default \code{TRUE}).}

\item{legend.title}{Character; legend title.  \code{NULL} derives it from the
measure.}

\item{title}{Character; plot title.}

\item{caption}{Character; caption under the plot.  \code{TRUE} (default)
states that the midbrain nuclei are schematic, \code{FALSE} omits it.}

\item{file}{One of: \code{character(0)} (default, auto-name); \code{NULL}
(return the plot, draw nothing); or an explicit file path.}

\item{format}{Character; \code{"png"} (default) or \code{"pdf"}.}

\item{width, height}{Integer; output size in pixels.  \code{NULL}
auto-computes.}
}
\value{
Invisibly, the file path of the saved plot (or \code{NULL} when
  \code{file = NULL}).
}
\description{
Brain map of pooled regional effects
}
\details{
Requires the suggested packages \pkg{ggseg}, \pkg{ggplot2}, \pkg{sf} and
\pkg{MetBrewer}.

Only the analyses whose labels resolve to a region of the slice are coloured;
the rest of the slice stays neutral grey, and unmatched analyses are reported
in a warning.
}
