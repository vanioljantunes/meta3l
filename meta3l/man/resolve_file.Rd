% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest_helpers.R
\name{resolve_file}
\alias{resolve_file}
\title{Resolve output file path for forest plot (internal)}
\usage{
resolve_file(x, file, format, suffix = "")
}
\arguments{
\item{x}{A \code{meta3l_result} object (must have a \code{name} field).}

\item{file}{One of: \code{NULL} (display only, return \code{NULL});
\code{character(0)} (sentinel for auto-name); or a character string
(returned as-is).}

\item{format}{Character string; file extension, e.g. \code{"png"} or
\code{"pdf"}.}

\item{suffix}{Character string; optional suffix appended to the base name
with an underscore separator before the file extension.  For example,
\code{suffix = "subgroup_drug"} produces
\code{"{name}_subgroup_drug.{format}"}.  Defaults to \code{""} (no
suffix), which preserves backward-compatible behaviour.}
}
\value{
A character string (file path) or \code{NULL}.
}
\description{
Resolves the file path for saving the forest plot based on a sentinel
\code{file} argument, the result object's \code{name} field, the active
\code{meta3l.mwd} option, the desired \code{format}, and an optional
\code{suffix} for Phase 3 filename patterns.
}
\keyword{internal}
