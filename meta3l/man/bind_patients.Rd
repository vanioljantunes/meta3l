% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metabind.meta3L.R
\name{bind_patients}
\alias{bind_patients}
\title{Count patients behind a set of effect sizes without double counting (internal)}
\usage{
bind_patients(dat, cluster, measure, method = c("max", "sum"))
}
\arguments{
\item{dat}{Data frame; the rows to count (already subset).}

\item{cluster}{Character string; name of the cluster column in \code{dat}.}

\item{measure}{Character string; the effect size measure, used to pick the
sample-size columns.}

\item{method}{\code{"max"} (default) or \code{"sum"}.}
}
\value{
A named numeric vector \code{c(e = ..., c = ...)}.  For single-arm
  measures the control element is \code{NA_real_}.
}
\description{
A three-level data set repeats the same patients across the effect sizes of
one cluster (e.g. left and right measurements of the same participants), so
summing the sample-size column over rows inflates the count.  With
\code{method = "max"} the largest sample size reported by each cluster is
taken once and those cluster totals are summed; \code{method = "sum"}
restores the naive row-wise sum for data sets whose rows really are disjoint
patient groups.
}
\keyword{internal}
