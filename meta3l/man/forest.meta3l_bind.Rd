% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metabind.meta3L.R
\name{forest.meta3l_bind}
\alias{forest.meta3l_bind}
\title{Summary forest plot for combined three-level meta-analyses}
\usage{
\method{forest}{meta3l_bind}(
  x,
  analysis.lab = "Outcome",
  refline = NULL,
  xlim = NULL,
  xlim.trim = 0.95,
  at = NULL,
  xlab = NULL,
  title = NULL,
  add.text = TRUE,
  showdata = TRUE,
  showk = TRUE,
  showi2 = TRUE,
  showi2.parts = TRUE,
  shade = "zebra",
  colshade = rgb(0.92, 0.92, 0.92),
  squaresize = 1,
  digits = 2L,
  digits.data = 1L,
  file = character(0),
  format = "png",
  width = NULL,
  height = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{meta3l_bind} object returned by \code{\link{metabind}}.}

\item{analysis.lab}{Character string; header of the first column.  Defaults
to \code{"Outcome"}.}

\item{refline}{Numeric scalar; x position of the vertical reference line.
\code{NULL} auto-derives from the measure.}

\item{xlim}{Numeric vector of length 2; x-axis limits.  \code{NULL}
auto-computes from the pooled estimates and their confidence limits.}

\item{xlim.trim}{Numeric in (0, 1]; quantile of the confidence bounds the
automatic x-axis has to cover.  The default \code{0.95} keeps one very
imprecise subgroup from stretching the axis until every other interval is
unreadable; intervals reaching past the limits get arrow heads.  Set to
\code{1} to cover every bound, or pass \code{xlim} to fix the limits.}

\item{at}{Numeric vector; tick positions.  \code{NULL} uses
\code{pretty(xlim, n = 5)}.}

\item{xlab}{Character string; x-axis label.  \code{NULL} omits it.}

\item{title}{Character string; plot title drawn below the axis.}

\item{add.text}{Footnote describing the model, drawn under the plot.
\code{TRUE} (default) composes it from the object, \code{FALSE} omits it,
and a character vector is printed verbatim, one line per element.}

\item{showdata}{Logical; show the per-arm data columns (default
\code{TRUE}).  For two-arm measures these are mean, SD and total for each
group, headed by the group names; single-arm measures get one total column.}

\item{showk}{Logical; show the "Studies" and "Effects" columns
(default \code{TRUE}).}

\item{showi2}{Logical; show the I-squared columns (default \code{TRUE}).}

\item{showi2.parts}{Logical; split I-squared into between- and
within-cluster columns next to the total (default \code{TRUE}).}

\item{shade}{One of \code{"zebra"} (default, alternate estimate rows),
\code{"category"} (one band per subgroup category), \code{"block"}
(alternate analyses) or \code{"none"}.}

\item{colshade}{Colour used for shading.}

\item{squaresize}{Numeric scaling factor for the subgroup-level squares.}

\item{digits}{Integer; digits for estimates and confidence limits.}

\item{digits.data}{Integer; digits for the mean and SD columns.}

\item{file}{One of: \code{character(0)} (default, auto-name); \code{NULL}
(display only); or an explicit file path.}

\item{format}{Character; \code{"png"} (default) or \code{"pdf"}.}

\item{width}{Integer; output width in pixels.  \code{NULL} auto-computes.}

\item{height}{Integer; output height in pixels.  \code{NULL} auto-computes.}

\item{...}{Currently ignored.}
}
\value{
Invisibly returns the file path of the saved plot, or \code{NULL}
  when \code{file = NULL}.
}
\description{
Draws one mark per pooled estimate collected by \code{\link{metabind}}: a
diamond for the pooled estimate of each analysis and a square for each
subgroup level.  No study-level rows are drawn - use
\code{\link{forest_subgroup}} for those.
}
\details{
Each analysis opens with a header row carrying its name and, when the object
was built with \code{subgroup}, the p-value of the omnibus test for subgroup
differences of every category in brackets.
}
