% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest.meta3L.R
\name{forest.meta3L}
\alias{forest.meta3L}
\title{Forest plot for three-level meta-analysis results}
\usage{
\method{forest}{meta3L}(
  x,
  ilab = NULL,
  ilab.lab = NULL,
  sortvar = NULL,
  refline = NULL,
  xlim = NULL,
  at = NULL,
  xlab = NULL,
  showweights = TRUE,
  colshade = rgb(0.92, 0.92, 0.92),
  squaresize = 1,
  file = character(0),
  format = "png",
  width = NULL,
  height = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{meta3l_result} object returned by \code{\link{meta3L}}.}

\item{ilab}{Character vector of column names from \code{x$data} to display
as annotation columns to the left of the CI axis.  Defaults to
\code{NULL} (no annotation columns).}

\item{ilab.lab}{Character vector of column headers for \code{ilab} columns.
If \code{NULL} (default), the column names from \code{ilab} are used.}

\item{sortvar}{Character string; name of a column in \code{x$data} to sort
studies by (ascending).  \code{NULL} (default) preserves original order.}

\item{refline}{Numeric scalar; x position for a vertical reference line.
\code{NULL} (default) derives the line automatically from \code{x$measure}
via \code{auto_refline()}: \code{NULL} for PLO/PAS, \code{0} for SMD/MD,
\code{1} for RR/OR.  Pass \code{NA} to suppress the line explicitly.}

\item{xlim}{Numeric vector of length 2; x-axis limits for the CI panel.
\code{NULL} (default) auto-computes via \code{auto_xlim()}.}

\item{at}{Numeric vector of x positions for axis tick marks.  \code{NULL}
(default) uses \code{pretty(xlim, n = 5)}.}

\item{xlab}{Character string; x-axis label.  \code{NULL} (default) omits
the label.}

\item{showweights}{Logical; if \code{TRUE} (default) a weight column is
displayed showing percentage contribution of each study.}

\item{colshade}{Colour used for alternating row shading (zebra stripes).
Defaults to \code{rgb(0.92, 0.92, 0.92)}.}

\item{squaresize}{Numeric scaling factor applied to the weight-proportional
study squares.  Defaults to \code{1}.}

\item{file}{One of: \code{character(0)} (default, auto-name from
\code{x$name} and \code{meta3l.mwd} option); \code{NULL} (display only);
or a character string (explicit file path).}

\item{format}{Character string; output format — \code{"png"} (default) or
\code{"pdf"}.}

\item{width}{Integer; output width in pixels (PNG) or 1/300 inches (PDF).
\code{NULL} auto-computes via \code{auto_dims()}.}

\item{height}{Integer; output height in pixels.  \code{NULL} auto-computes.}

\item{...}{Currently ignored; reserved for future extension.}
}
\value{
Invisibly returns the file path (character string) of the saved
  plot, or \code{NULL} if \code{file = NULL} (display only).
}
\description{
Produces a publication-quality forest plot from a \code{meta3l_result}
object using grid graphics.  By default the plot is saved to a PNG file
whose name is derived from \code{x$name} and the \code{meta3l.mwd} option.
Pass \code{file = NULL} for display-only mode (renders in RStudio viewer).
}
\examples{
\dontrun{
d <- data.frame(
  studlab = rep(c("Smith, 2020", "Jones, 2021"), each = 3),
  xi = c(10, 12, 8, 15, 11, 9),
  ni = c(50, 55, 48, 60, 52, 47)
)
r <- meta3L(d, slab = "studlab", xi = "xi", ni = "ni",
            measure = "PLO", name = "my_analysis")
forest.meta3L(r)                                   # auto-named PNG
forest.meta3L(r, file = "my_plot.pdf", format = "pdf")
forest.meta3L(r, ilab = c("xi", "ni"),
              ilab.lab = c("Events", "Total"))
}

}
