% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest_subgroup.meta3L.R
\name{forest_subgroup}
\alias{forest_subgroup}
\alias{forest_subgroup.meta3L}
\title{Subgroup forest plot for three-level meta-analysis results}
\usage{
forest_subgroup(x, ...)

\method{forest_subgroup}{meta3L}(
  x,
  subgroup,
  overall = TRUE,
  ilab = NULL,
  ilab.lab = NULL,
  sortvar = NULL,
  refline = NULL,
  xlim = NULL,
  at = NULL,
  xlab = NULL,
  title = x$name,
  showweights = TRUE,
  colshade = rgb(0.92, 0.92, 0.92),
  squaresize = 1,
  file = character(0),
  format = "png",
  width = NULL,
  height = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{meta3l_result} object returned by \code{\link{meta3L}}.}

\item{...}{Currently ignored.}

\item{subgroup}{Character string; name of the column in \code{x$data} to
use as the grouping variable.  The column must be present in
\code{x$data}.}

\item{overall}{Logical; if \code{TRUE} (default) an overall summary diamond
is drawn below all subgroup sections.}

\item{ilab}{Character vector of column names from \code{x$data} to display
as annotation columns.  Defaults to \code{NULL}.}

\item{ilab.lab}{Character vector of column headers for \code{ilab} columns.
Defaults to \code{NULL} (uses column names).}

\item{sortvar}{Character string; name of a column to sort studies by
within each subgroup.  \code{NULL} preserves original order.}

\item{refline}{Numeric scalar; x position for a vertical reference line.
\code{NULL} auto-derives from \code{x$measure}.}

\item{xlim}{Numeric vector of length 2; x-axis limits.  \code{NULL}
auto-computes.}

\item{at}{Numeric vector; tick mark positions.  \code{NULL} uses
\code{pretty(xlim, n = 5)}.}

\item{xlab}{Character string; x-axis label.  \code{NULL} omits.}

\item{showweights}{Logical; display weight column (default \code{TRUE}).}

\item{colshade}{Colour for alternating row shading.
Defaults to \code{rgb(0.92, 0.92, 0.92)}.}

\item{squaresize}{Numeric scaling factor for study squares.
Defaults to \code{1}.}

\item{file}{One of: \code{character(0)} (default, auto-name);
\code{NULL} (display only); or an explicit character file path.}

\item{format}{Character; \code{"png"} (default) or \code{"pdf"}.}

\item{width}{Integer; output width in pixels.  \code{NULL} auto-computes.}

\item{height}{Integer; output height in pixels.  \code{NULL} auto-computes.}
}
\value{
Invisibly returns the file path (character string) of the saved
  plot, or \code{NULL} if \code{file = NULL} (display only).
}
\description{
Produces a grouped forest plot in which studies are arranged by a subgroup
variable.  Each subgroup section has a bold header row, study rows with
CI lines and weight-proportional squares, and (when the subgroup contains
two or more clusters) a subgroup summary diamond annotated with per-subgroup
I-squared.  An omnibus Q-test for subgroup differences is shown in the
footer.
}
